apiVersion: v1
kind: Service
metadata:
  name: fe-video-compress
  namespace: video-compress
spec:
  type: NodePort
  selector:
    app: fe-video-compress
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
