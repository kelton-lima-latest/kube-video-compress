apiVersion: v1
kind: Secret
metadata:
  name: secrets-video-compress
  namespace: video-compress
type: opaque
data:
  POSTGRES_USER: @@@
  POSTGRES_PASSWORD: @@@
  POSTGRES_DB: @@@
  POSTGRES_PORT: @@@
  REDIS_PORT: @@@
